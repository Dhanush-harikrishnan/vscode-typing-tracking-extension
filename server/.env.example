# Environment Variables for VS Code Typing Tracker Server

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/vscode_tracker
# For MongoDB Atlas (cloud - free tier):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vscode_tracker?retryWrites=true&w=majority

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins (for production use your Vercel URLs)
CORS_ORIGIN=https://frotend-vscode-extension.vercel.app,https://frotend-vscode-extension-p02mo2u2y-dhanush-hs-projects.vercel.app,http://localhost:3001
# For development, use * to allow all origins:
# CORS_ORIGIN=*
